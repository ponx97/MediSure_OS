# Server
PORT=4010
NODE_ENV=production

# Postgres (shared container on mediapp_mediapp_net)
DATABASE_URL=postgresql://mediapp_user:Faith4tila@mediapp_db:5432/medisure_db

# Auth
JWT_ACCESS_SECRET=change_me_access_secret
JWT_REFRESH_SECRET=change_me_refresh_secret
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Bootstrap admin (created if no admin exists)
ADMIN_EMAIL=admin@medisure.local
ADMIN_PASSWORD=ChangeMeNow123!

# Gemini (server-side only)
GEMINI_API_KEY=your_gemini_key_here

# CORS
CORS_ORIGINS=https://medisureos.ithinksys.co.zw,http://localhost:5173
